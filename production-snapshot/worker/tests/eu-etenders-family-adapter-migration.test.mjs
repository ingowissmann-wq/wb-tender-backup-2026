import assert from "node:assert/strict";import fs from "node:fs";import test from "node:test";
const up=fs.readFileSync(new URL("../migrations/150_eu_funding_etenders_family_adapters.sql",import.meta.url),"utf8"),down=fs.readFileSync(new URL("../migrations/150_eu_funding_etenders_family_adapters.down.sql",import.meta.url),"utf8");
test("migration binds only exact observed EU and Irish portals to internal adapters",()=>{for(const value of ["7b624353-e099-4e6f-9a50-6a778e5ed892","003f1d5d-30ee-4b53-b020-9bec6c416ba5","eu-funding-tenders","etenders-ireland","INTERNAL_CONTRACT_VALIDATED","EXTERNAL_VALIDATION_PENDING"])assert.match(up,new RegExp(value));assert.match(up,/'bindingSubmissionEnabled',false/);assert.doesNotMatch(up,/adapter_validation_status='PRODUCTION_VALIDATED'/)});
test("rollback disables implementations and retains evidence",()=>{assert.match(down,/enabled=false,validation_status='ADAPTER_REPAIR_REQUIRED'/);assert.match(down,/'retainedEvidence',true/);assert.doesNotMatch(down,/DELETE FROM tender\./)});
