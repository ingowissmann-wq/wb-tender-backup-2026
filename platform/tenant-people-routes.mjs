import {TenantPeople} from './tenant-people.mjs';
import {requireTenantContext} from './tenant-context.mjs';
import {requireSaasModule} from './saas-platform.mjs';
import {MODULE_KEYS} from './saas-catalog.mjs';
export function registerTenantPeopleRoutes(app,{pool,authenticate,csrf}){
 const people=new TenantPeople(pool),isAdmin=req=>['OWNER','ADMIN'].includes(req.identity?.saas?.role),guards=[authenticate,async(req,reply)=>requireTenantContext(req,reply),requireSaasModule(MODULE_KEYS.PEOPLE)],admin=async(req,reply)=>{if(!isAdmin(req))return reply.code(403).send({error:'tenant_admin_required'})};
 const safe=handler=>async(req,reply)=>{reply.header('Cache-Control','no-store');try{return await handler(req,reply)}catch(error){return reply.code(error.statusCode||(error.code==='23505'?409:503)).send({error:error.statusCode?error.message:error.code==='23505'?'employee_or_task_conflict':'people_temporarily_unavailable'})}};
 app.get('/api/tenant-portal/people/employees',{preHandler:[...guards,admin]},safe(req=>people.list(req.tenant)));
 app.get('/api/tenant-portal/people/employees/:id',{preHandler:[...guards,admin]},safe(req=>people.detail(req.tenant,req.params.id)));
 app.get('/api/tenant-portal/people/me',{preHandler:guards},safe(async req=>({...await people.self(req.tenant),actorUserId:req.identity.userId})));
 app.post('/api/tenant-portal/people/employees',{preHandler:[...guards,admin,csrf]},safe(async(req,reply)=>{const result=await people.create(req.tenant,req.body);return reply.code(result.idempotent?200:201).send(result.item)}));
 app.patch('/api/tenant-portal/people/employees/:id',{preHandler:[...guards,admin,csrf]},safe(req=>people.update(req.tenant,req.params.id,req.body)));
 app.post('/api/tenant-portal/people/employees/:id/onboarding',{preHandler:[...guards,admin,csrf]},safe(async(req,reply)=>{const result=await people.createTask(req.tenant,req.params.id,req.body);return reply.code(result.idempotent?200:201).send(result.item)}));
 app.patch('/api/tenant-portal/people/onboarding/:id',{preHandler:[...guards,csrf]},safe(req=>people.decide(req.tenant,req.params.id,req.body,{admin:isAdmin(req)})));
 app.get('/saas/assets/people.js',{preHandler:[authenticate]},async(_,reply)=>reply.type('text/javascript').send(script));
 const page=(mode)=>`<!doctype html><html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Mitarbeiterportal · WB-Tender</title><link rel="stylesheet" href="/saas/assets/commercial.css"><script src="/saas/assets/people.js" defer></script></head><body><main class="panel" data-mode="${mode}"><h1>${mode==='admin'?'Mitarbeiter und Onboarding':'Mein Mitarbeiterportal'}</h1><p id="status" role="status"></p>${mode==='admin'?'<h2>Mitarbeiterprofil anlegen</h2><form id="create"><label>Aktives Benutzerkonto<select name="userId" required></select></label><label>Anzeigename<input name="displayName" minlength="2" maxlength="160" required></label><label>Geschäftliche E-Mail-Adresse<input name="workEmail" type="email" maxlength="254"></label><label>Personalnummer<input name="employeeNumber" maxlength="80"></label><label>Funktion<input name="jobTitle" maxlength="160"></label><label>Team<input name="teamName" maxlength="160"></label><label>Eintrittsdatum<input name="startDate" type="date"></label><button>Profil anlegen</button></form>':''}<div id="employees"></div><p><a href="/saas/app/me">Mein Profil</a> · <a href="/saas/account">Mein Konto</a></p></main></body></html>`;
 app.get('/saas/app/people',{preHandler:guards},async(req,reply)=>isAdmin(req)?reply.header('Cache-Control','no-store').type('text/html').send(page('admin')):reply.redirect('/saas/app/me'));
 app.get('/saas/app/me',{preHandler:guards},async(_,reply)=>reply.header('Cache-Control','no-store').type('text/html').send(page('self')));
}
const script=`const mode=document.querySelector('[data-mode]').dataset.mode,root=document.querySelector('#employees'),status=document.querySelector('#status');const node=(tag,text)=>{const n=document.createElement(tag);n.textContent=text;return n};async function api(url,body,method='POST'){const r=await fetch(url,body?{method,headers:{'content-type':'application/json','x-csrf-token':decodeURIComponent(document.cookie.split('; ').find(x=>x.startsWith('wb_csrf='))?.slice(8)||'')},body:JSON.stringify(body)}:{}),data=await r.json();if(!r.ok)throw new Error(data.error||data.message);return data}let members=[];function field(form,name,label,value='',type='text',required=false){const wrap=node('label',label),input=document.createElement('input');input.name=name;input.type=type;input.value=value||'';input.required=required;input.maxLength=240;wrap.append(input);form.append(wrap);return input}function memberSelect(form,name,label){const wrap=node('label',label),select=document.createElement('select');select.name=name;select.add(new Option('Noch nicht zugewiesen',''));for(const m of members)select.add(new Option(m.email,m.user_id));wrap.append(select);form.append(wrap);return select}
function taskForm(task,allowed,refresh){const section=document.createElement('section');section.dataset.task=task.id;section.append(node('h4',task.title),node('p',({OPEN:'Offen',DONE:'Erledigt',NOT_APPLICABLE:'Nicht erforderlich'}[task.status])+(task.due_at?' · Frist '+String(task.due_at).slice(0,10):'')));if(!allowed)return section;const form=document.createElement('form'),select=document.createElement('select');select.name='status';for(const [v,t] of [['OPEN','Offen'],['DONE','Erledigt'],['NOT_APPLICABLE','Nicht erforderlich']])select.add(new Option(t,v));select.value=task.status;form.append(select);field(form,'reason','Begründung','', 'text',true).minLength=10;const label=node('label',''),confirm=document.createElement('input');confirm.type='checkbox';confirm.required=true;label.append(confirm,document.createTextNode('Aufgabe und Status geprüft'));form.append(label,node('button','Aufgabenstatus speichern'));form.addEventListener('submit',async event=>{event.preventDefault();try{await api('/api/tenant-portal/people/onboarding/'+task.id,{status:select.value,expectedRevision:task.revision,reason:form.elements.reason.value,confirmed:confirm.checked},'PATCH');status.textContent='Onboardingstatus gespeichert.';await refresh()}catch(error){status.textContent=error.message}});section.append(form);return section}
async function showTasks(employee,area){const detail=await api('/api/tenant-portal/people/employees/'+employee.id);area.replaceChildren();for(const task of detail.tasks)area.append(taskForm(task,true,()=>showTasks(employee,area)));const form=document.createElement('form');form.dataset.newTask=employee.id;field(form,'title','Neue Onboardingaufgabe','','text',true).minLength=3;memberSelect(form,'assigneeUserId','Verantwortlich');field(form,'dueAt','Frist','','date');form.append(node('button','Onboardingaufgabe anlegen'));form.addEventListener('submit',async event=>{event.preventDefault();try{form.dataset.id||=crypto.randomUUID();await api('/api/tenant-portal/people/employees/'+employee.id+'/onboarding',{id:form.dataset.id,title:form.elements.title.value,assigneeUserId:form.elements.assigneeUserId.value||null,dueAt:form.elements.dueAt.value||null});status.textContent='Onboardingaufgabe angelegt.';await showTasks(employee,area)}catch(error){status.textContent=error.message}});area.append(form)}
async function load(){root.replaceChildren();if(mode==='self'){const data=await api('/api/tenant-portal/people/me');if(data.employee){root.append(node('h2',data.employee.display_name));const dl=document.createElement('dl');for(const [key,label] of [['work_email','Geschäftliche E-Mail'],['employee_number','Personalnummer'],['job_title','Funktion'],['team_name','Team'],['employment_status','Beschäftigungsstatus']]){dl.append(node('dt',label),node('dd',data.employee[key]||'Nicht angegeben'))}root.append(dl)}else root.append(node('p','Noch kein Mitarbeiterprofil zugeordnet.'));for(const task of data.tasks)root.append(taskForm(task,task.assignee_user_id===data.actorUserId,load));return;}
const data=await api('/api/tenant-portal/people/employees');for(const employee of data.items){const section=document.createElement('section');section.dataset.employee=employee.id;section.append(node('h2',employee.display_name));const edit=document.createElement('form');field(edit,'displayName','Anzeigename',employee.display_name,'text',true);field(edit,'jobTitle','Funktion',employee.job_title);field(edit,'teamName','Team',employee.team_name);field(edit,'phone','Telefon',employee.phone);const select=document.createElement('select');select.name='employmentStatus';for(const [v,t] of [['ONBOARDING','Onboarding'],['ACTIVE','Aktiv'],['LEAVE','Abwesend'],['INACTIVE','Inaktiv']])select.add(new Option(t,v));select.value=employee.employment_status;edit.append(select,node('button','Profil speichern'));edit.addEventListener('submit',async event=>{event.preventDefault();try{await api('/api/tenant-portal/people/employees/'+employee.id,{displayName:edit.elements.displayName.value,jobTitle:edit.elements.jobTitle.value,teamName:edit.elements.teamName.value,phone:edit.elements.phone.value,employmentStatus:select.value,expectedRevision:employee.revision},'PATCH');status.textContent='Mitarbeiterprofil gespeichert.';await load()}catch(error){status.textContent=error.message}});section.append(edit);const button=node('button','Onboarding öffnen'),area=document.createElement('div');button.type='button';button.addEventListener('click',()=>showTasks(employee,area).catch(error=>status.textContent=error.message));section.append(button,area);root.append(section)}}
async function init(){if(mode==='admin'){members=(await api('/api/tenant-portal/control/members')).items.filter(x=>x.status==='ACTIVE');const form=document.querySelector('#create');form.elements.userId.add(new Option('Benutzer auswählen',''));for(const m of members)form.elements.userId.add(new Option(m.email,m.user_id));form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;try{form.dataset.id||=crypto.randomUUID();await api('/api/tenant-portal/people/employees',{id:form.dataset.id,userId:form.elements.userId.value,displayName:form.elements.displayName.value,workEmail:form.elements.workEmail.value,employeeNumber:form.elements.employeeNumber.value,jobTitle:form.elements.jobTitle.value,teamName:form.elements.teamName.value,startDate:form.elements.startDate.value||null});delete form.dataset.id;form.reset();status.textContent='Mitarbeiterprofil angelegt.';await load()}catch(error){status.textContent=error.message}finally{button.disabled=false}});}await load()}init().catch(error=>status.textContent=error.message);`;
