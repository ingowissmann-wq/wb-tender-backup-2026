import {TenantDocumentReview} from './tenant-document-review.mjs';
import {requireTenantContext} from './tenant-context.mjs';
import {requireSaasModule} from './saas-platform.mjs';
import {MODULE_KEYS} from './saas-catalog.mjs';
export function registerTenantDocumentReviewRoutes(app,{pool,storage,authenticate,csrf}){
 const review=new TenantDocumentReview(pool,storage);
 const admin=async(req,reply)=>{if(!['OWNER','ADMIN'].includes(req.identity?.saas?.role))return reply.code(403).send({error:'tenant_admin_required'})};
 const guards=[authenticate,async(req,reply)=>requireTenantContext(req,reply),requireSaasModule(MODULE_KEYS.TENDER_AUTOPILOT),admin];
 const safe=fn=>async(req,reply)=>{reply.header('Cache-Control','no-store');try{return await fn(req,reply)}catch(error){const known=/^document_review_/.test(error.message);return reply.code(known?(error.statusCode||400):503).send({error:known?error.message:'document_review_operation_failed'})}};
 app.get('/api/tenant-portal/lot-assignments/:id/document-reviews',{preHandler:guards},safe(req=>review.get(req.tenant,req.params.id)));
 app.post('/api/tenant-portal/lot-assignments/:id/document-reviews',{preHandler:[...guards,csrf],bodyLimit:16384},safe(req=>review.analyze(req.tenant,req.params.id,req.body)));
 app.post('/api/tenant-portal/document-reviews/:id/preview',{preHandler:[...guards,csrf],bodyLimit:262144},safe(req=>review.preview(req.tenant,req.params.id,req.body)));
 app.post('/api/tenant-portal/document-reviews/:id/confirm',{preHandler:[...guards,csrf],bodyLimit:524288},safe(req=>review.confirm(req.tenant,req.params.id,req.body)));
 app.get('/saas/assets/document-review.js',{preHandler:[authenticate]},async(_,reply)=>reply.type('text/javascript').send(script));
 app.get('/saas/app/document-review',{preHandler:guards},async(_,reply)=>reply.header('Cache-Control','no-store').type('text/html').send(`<!doctype html><html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Unterlagen prüfen · WB-Tender</title><link rel="stylesheet" href="/saas/assets/commercial.css"><script src="/saas/assets/document-review.js" defer></script></head><body><main class="panel"><h1>Vergabeunterlagen und Nachweise prüfen</h1><p>Wählen Sie die vollständigen, aktuellen Unterlagen für dieses Los. Die automatische Erkennung unterstützt Ihre Prüfung. Ergänzen Sie übersehene Pflichten mit einem wörtlichen Beleg aus der Quelldatei.</p><form id="upload"><label>Datei <input name="file" type="file" required accept=".pdf,.docx,.xlsx,.csv,.xml"></label><button>Datei hochladen</button></form><p id="status" role="status"></p><form id="analyze"><fieldset id="sources"><legend>Vergabeunterlagen</legend></fieldset><label><input type="checkbox" name="scope" required>Diese Unterlagen gehören zum ausgewählten Los und enthalten die aktuellen Nachträge.</label><button>Unterlagen analysieren</button></form><section id="review"></section><a href="/saas/app/lot-assignments">Losbearbeitung</a></main></body></html>`));
}
const script=`const assignment=new URLSearchParams(location.search).get('assignment'),status=document.querySelector('#status'),section=document.querySelector('#review');let data,current,requirements,additions=[];
const node=(tag,text)=>{const n=document.createElement(tag);if(text)n.textContent=text;return n};
async function api(path,body){const token=decodeURIComponent(document.cookie.split('; ').find(x=>x.startsWith('wb_csrf='))?.slice(8)||'');const r=await fetch(path,body?{method:'POST',headers:{'Content-Type':'application/json','x-csrf-token':token},body:JSON.stringify(body)}:{});const d=await r.json();if(!r.ok)throw new Error(d.error);return d}
function fileSelect(files){const select=node('select');select.append(new Option('Datei auswählen',''));for(const f of files)select.append(new Option(f.filename,f.id));return select}
function render(){section.replaceChildren();if(!current)return;section.append(node('h2','Prüfstand '+current.version+' · '+(current.status==='REVIEWED'?'Prüfung bestätigt':'Prüfung offen')));const form=node('form');for(const req of requirements){const box=node('fieldset');box.dataset.key=req.requirementKey;box.append(node('legend',req.title),node('blockquote',req.sourceExcerpt),node('p',req.sourceReference+(req.sourcePage?' · Seite '+req.sourcePage:'')));const state=node('select');state.name='decision';state.append(new Option('Nachweis fachlich geprüft','VALIDATED'),new Option('Für dieses Angebot nicht erforderlich','NOT_REQUIRED'));const evidence=fileSelect(data.files);evidence.name='evidence';const reason=node('textarea');reason.name='reason';reason.required=true;reason.minLength=10;reason.maxLength=1000;reason.placeholder='Prüfung oder Ausnahme begründen';box.append(state,evidence,reason);form.append(box)}const label=node('label'),coverage=node('input');coverage.type='checkbox';coverage.name='coverage';coverage.required=true;label.append(coverage,document.createTextNode('Ich habe alle Vergabeunterlagen und Nachträge geprüft, fehlende Pflichten ergänzt und die angegebenen Nachweise fachlich kontrolliert.'));const button=node('button','Dokumentenprüfung bestätigen');form.append(label,button);form.onsubmit=async e=>{e.preventDefault();button.disabled=true;try{form.dataset.requestId||=crypto.randomUUID();await api('/api/tenant-portal/document-reviews/'+current.id+'/confirm',{id:form.dataset.requestId,coverageConfirmed:coverage.checked,additions,decisions:[...form.querySelectorAll('fieldset')].map(x=>({key:x.dataset.key,status:x.querySelector('[name=decision]').value,fileId:x.querySelector('[name=evidence]').value,reason:x.querySelector('textarea').value}))});status.textContent='Dokumentenprüfung gespeichert.';await load()}catch(error){status.textContent=error.message;button.disabled=false}};section.append(form);
const add=node('form');add.append(node('h3','Fehlende Pflicht ergänzen'));const source=fileSelect(data.files.filter(x=>current.source_manifest.some(y=>y.id===x.id))),title=node('input'),quote=node('textarea');title.required=true;title.placeholder='Bezeichnung der Pflicht';quote.required=true;quote.placeholder='Wörtliches Zitat aus der Quelldatei';add.append(source,title,quote,node('button','Quellenbeleg prüfen und Pflicht ergänzen'));add.onsubmit=async e=>{e.preventDefault();try{const next=[...additions,{fileId:source.value,title:title.value,quote:quote.value}];requirements=await api('/api/tenant-portal/document-reviews/'+current.id+'/preview',{additions:next});additions=next;render()}catch(error){status.textContent=error.message}};section.append(add)}
async function load(){data=await api('/api/tenant-portal/lot-assignments/'+assignment+'/document-reviews');const sources=document.querySelector('#sources');sources.replaceChildren(node('legend','Vergabeunterlagen'));for(const f of data.files){const label=node('label'),check=node('input');check.type='checkbox';check.value=f.id;check.name='file';label.append(check,document.createTextNode(f.filename));sources.append(label)}current=data.reviews[0];requirements=current?.requirements||[];additions=[];render()}
document.querySelector('#upload').onsubmit=async e=>{e.preventDefault();try{const file=e.target.file.files[0];if(!file||file.size>10*1024*1024)throw new Error('Datei muss kleiner als 10 MiB sein.');const bytes=new Uint8Array(await file.arrayBuffer());let binary='';for(let offset=0;offset<bytes.length;offset+=8192)binary+=String.fromCharCode(...bytes.subarray(offset,offset+8192));await api('/api/tenant-portal/modules/docs/files',{filename:file.name,mediaType:file.type||'application/octet-stream',contentBase64:btoa(binary)});status.textContent='Datei gespeichert.';await load()}catch(error){status.textContent=error.message}};
document.querySelector('#analyze').onsubmit=async e=>{e.preventDefault();try{e.target.dataset.requestId||=crypto.randomUUID();await api('/api/tenant-portal/lot-assignments/'+assignment+'/document-reviews',{id:e.target.dataset.requestId,confirmedLotScope:e.target.scope.checked,fileIds:[...e.target.querySelectorAll('[name=file]:checked')].map(x=>x.value)});delete e.target.dataset.requestId;status.textContent='Analyse gespeichert. Prüfen Sie die erkannten Pflichten und ergänzen Sie fehlende.';await load()}catch(error){status.textContent=error.message}};load().catch(error=>status.textContent=error.message);`;
