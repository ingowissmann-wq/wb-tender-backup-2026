// Parameterbezogene Lade- und Abort-Logik liegt in configuration.js. Dieses
// separate Asset bleibt absichtlich ohne globalen Render-Wrapper: ein Wrapper
// konnte veraltete Render-Aufrufe nur serialisieren, aber nicht verwerfen.
