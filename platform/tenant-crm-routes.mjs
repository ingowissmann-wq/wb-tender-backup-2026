import {TenantCrm} from './tenant-crm.mjs';
import {requireTenantContext} from './tenant-context.mjs';
import {requireSaasModule} from './saas-platform.mjs';
import {MODULE_KEYS} from './saas-catalog.mjs';
export function registerTenantCrmRoutes(app,{pool,authenticate,csrf}){
 const crm=new TenantCrm(pool),guards=[authenticate,async(req,reply)=>requireTenantContext(req,reply),requireSaasModule(MODULE_KEYS.CRM)];
 const safe=handler=>async(req,reply)=>{reply.header('Cache-Control','no-store');try{return await handler(req,reply)}catch(error){return reply.code(error.statusCode||503).send({error:error.statusCode?error.message:'crm_temporarily_unavailable'})}};
 app.get('/api/tenant-portal/crm/accounts',{preHandler:guards},safe(req=>crm.list(req.tenant,req.query.q)));
 app.get('/api/tenant-portal/crm/accounts/:id',{preHandler:guards},safe(req=>crm.detail(req.tenant,req.params.id)));
 for(const [kind,path] of [['account','accounts'],['contact','contacts']]){
  app.post('/api/tenant-portal/crm/'+path,{preHandler:[...guards,csrf]},safe(async(req,reply)=>{const result=await crm.save(req.tenant,kind,req.body);return reply.code(result.idempotent?200:201).send(result)}));
  app.patch('/api/tenant-portal/crm/'+path+'/:id',{preHandler:[...guards,csrf]},safe(req=>crm.save(req.tenant,kind,{...req.body,id:req.params.id},{update:true})));
 }
 app.get('/saas/assets/crm.js',{preHandler:[authenticate]},async(_,reply)=>reply.type('text/javascript').send(script));
 app.get('/saas/app/crm',{preHandler:guards},async(_,reply)=>reply.header('Cache-Control','no-store').type('text/html').send(`<!doctype html><html lang="de"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Kunden und Kontakte · WB-Tender</title><link rel="stylesheet" href="/saas/assets/commercial.css"><script src="/saas/assets/crm.js" defer></script></head><body><main class="panel"><h1>Kunden und Kontakte</h1><p id="status" role="status"></p><form id="search"><label>Kunden suchen<input name="q" maxlength="120"></label><button>Suchen</button></form><h2>Kundenakte anlegen</h2><form id="create"><label>Name des Interessenten oder Kunden<input name="name" minlength="2" maxlength="200" required></label><label>Vertriebsstand<select name="stage"><option value="PROSPECT">Interessent</option><option value="QUALIFIED">Qualifiziert</option><option value="PROPOSAL">Angebotsphase</option><option value="CUSTOMER">Kunde</option><option value="LOST">Nicht gewonnen</option></select></label><button>Kundenakte anlegen</button></form><div id="accounts"></div><p><a href="/saas/app/workflow">Aufgaben und Fristen</a> · <a href="/saas/app/management">Management</a></p></main></body></html>`));
}
const script=`const status=document.querySelector('#status'),root=document.querySelector('#accounts'),create=document.querySelector('#create'),search=document.querySelector('#search');const stages={PROSPECT:'Interessent',QUALIFIED:'Qualifiziert',PROPOSAL:'Angebotsphase',CUSTOMER:'Kunde',LOST:'Nicht gewonnen'};const node=(tag,text)=>{const n=document.createElement(tag);n.textContent=text;return n};async function api(url,body,method='POST'){const response=await fetch(url,body?{method,headers:{'content-type':'application/json','x-csrf-token':decodeURIComponent(document.cookie.split('; ').find(x=>x.startsWith('wb_csrf='))?.slice(8)||'')},body:JSON.stringify(body)}:{});const data=await response.json();if(!response.ok)throw new Error(data.error);return data}function field(form,name,label,value='',type='text'){const wrapper=node('label',label),input=document.createElement('input');input.name=name;input.type=type;input.value=value;input.maxLength=name==='email'?254:200;input.required=name!=='email';wrapper.append(input);form.append(wrapper);return input}function stageField(form,value){const label=node('label','Vertriebsstand'),select=document.createElement('select');select.name='stage';for(const [code,name] of Object.entries(stages))select.add(new Option(name,code));select.value=value;label.append(select);form.append(label);return select}
async function load(){const data=await api('/api/tenant-portal/crm/accounts?q='+encodeURIComponent(search.elements.q.value));root.replaceChildren();if(data.items.length>500)root.append(node('p','Mehr als 500 Treffer. Bitte die Suche eingrenzen.'));for(const account of data.items.slice(0,500)){const section=document.createElement('section');section.dataset.account=account.id;section.append(node('h2',account.name));const edit=document.createElement('form');field(edit,'name','Name',account.name);stageField(edit,account.stage);field(edit,'reason','Begründung');edit.append(node('button','Kundenstand speichern'));edit.addEventListener('submit',async event=>{event.preventDefault();try{await api('/api/tenant-portal/crm/accounts/'+account.id,{name:edit.elements.name.value,stage:edit.elements.stage.value,expectedRevision:account.revision,reason:edit.elements.reason.value},'PATCH');status.textContent='Kundenstand gespeichert.';await load()}catch(error){status.textContent=error.message}});section.append(edit);const details=node('button','Kontakte öffnen'),area=document.createElement('div');details.type='button';details.addEventListener('click',async()=>{try{await contacts(account,area)}catch(error){status.textContent=error.message}});section.append(details,area);root.append(section)}if(!data.items.length)root.append(node('p','Keine Kundenakten gefunden.'))}
async function contacts(account,area){const detail=await api('/api/tenant-portal/crm/accounts/'+account.id);area.replaceChildren(node('h3','Kontakte'));for(const contact of detail.contacts){const form=document.createElement('form');form.dataset.contact=contact.id;field(form,'name','Kontaktname',contact.name);field(form,'email','E-Mail-Adresse',contact.email||'','email');field(form,'reason','Begründung');form.append(node('button','Kontakt speichern'));form.addEventListener('submit',async event=>{event.preventDefault();try{await api('/api/tenant-portal/crm/contacts/'+contact.id,{accountId:account.id,name:form.elements.name.value,email:form.elements.email.value,expectedRevision:contact.revision,reason:form.elements.reason.value},'PATCH');status.textContent='Kontakt gespeichert.';await contacts(account,area)}catch(error){status.textContent=error.message}});area.append(form)}const form=document.createElement('form');form.dataset.newContact=account.id;field(form,'name','Neuer Kontaktname');field(form,'email','Neue E-Mail-Adresse','','email');form.append(node('button','Kontakt anlegen'));form.addEventListener('submit',async event=>{event.preventDefault();const button=form.querySelector('button');button.disabled=true;try{form.dataset.id||=crypto.randomUUID();await api('/api/tenant-portal/crm/contacts',{id:form.dataset.id,accountId:account.id,name:form.elements.name.value,email:form.elements.email.value});status.textContent='Kontakt angelegt.';await contacts(account,area)}catch(error){status.textContent=error.message;button.disabled=false}});area.append(form)}
create.addEventListener('submit',async event=>{event.preventDefault();const button=create.querySelector('button');button.disabled=true;try{create.dataset.id||=crypto.randomUUID();await api('/api/tenant-portal/crm/accounts',{id:create.dataset.id,name:create.elements.name.value,stage:create.elements.stage.value});delete create.dataset.id;create.reset();status.textContent='Kundenakte angelegt.';await load()}catch(error){status.textContent=error.message}finally{button.disabled=false}});search.addEventListener('submit',event=>{event.preventDefault();load().catch(error=>status.textContent=error.message)});load().catch(error=>status.textContent=error.message);`;
